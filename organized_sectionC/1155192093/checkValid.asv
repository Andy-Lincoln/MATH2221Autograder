function v=checkValid(M,i,j)
if i~=1
    if i~=2
    if i~=3
    v=0;
    display(Invalid I);
elseif j~=[1,2,3]
    v=0;
    display(Invalid J);
elseif M(i,j)~=0
    display(Already occupied);
    v=0;
else
    display(Valid input);
    v=1;
end
end
